# Pneumonia Detection using CNN in Python
We are building a deep learning system to detect pneumonia from chest X-ray images. Using a Convolutional Neural Network (CNN) based on the VGG16 architecture to improve diagnostic accuracy.
